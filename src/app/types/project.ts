export interface Project {
  title: string;
  description: string;
  status?: string;
  statusNote?: string;
  tags: string[];
  link: string;
  bg: string;
  border: string;
  shadow: string;
  tag: string;
  arrow: string;
}
